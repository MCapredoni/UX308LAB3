// Calculate the **volume** of a cube. Start with a variable that contains the **length** of the cube, and another variable that references the **height** in meters, followed by another variable for **width**

function volume(temperature){
    let volume = length * width * height;
    return volume;
}

export {volume}